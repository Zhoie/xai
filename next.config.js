/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // 如果您使用 Tailwind CSS，可以添加以下内容
  // 需要确保已安装相应的插件
  // future: {
  //   webpack5: true,
  // },
  // 添加您需要的其他配置
};